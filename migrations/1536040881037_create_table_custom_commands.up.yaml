- args:
    sql: CREATE TABLE "custom_commands"("command" text NOT NULL UNIQUE DEFAULT '""',
      "reply" text NOT NULL DEFAULT '""', PRIMARY KEY ("command") )
  type: run_sql
- args:
    name: custom_commands
  type: add_existing_table_or_view
